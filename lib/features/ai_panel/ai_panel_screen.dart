import 'package:flutter/material.dart';

class AiPanelScreen extends StatelessWidget {
  const AiPanelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AiPanelScreen'),
      ),
    );
  }
}
