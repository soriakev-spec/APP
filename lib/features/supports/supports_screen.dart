import 'package:flutter/material.dart';

class SupportsScreen extends StatelessWidget {
  const SupportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SupportsScreen'),
      ),
    );
  }
}
