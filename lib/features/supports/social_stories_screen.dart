import 'package:flutter/material.dart';

class SocialStoriesScreen extends StatelessWidget {
  const SocialStoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SocialStoriesScreen'),
      ),
    );
  }
}
